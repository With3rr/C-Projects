#pragma once
#include <iostream>
using namespace std;
class Punkt;
class Prostokont
{
	string nazwa;
	float x, y, szerokosc, wysokosc;

public:
	Prostokont(string = "brak", float=0, float=0, float=1, float=1);
	void wczytaj();
	friend void sedzia(Punkt& pkt, Prostokont& p);
};

