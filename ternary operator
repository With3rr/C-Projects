
#include <iostream>
using namespace std;

#include <iostream>
using namespace std;
inline int Max(int a, int b, int c)
{

	return a > b ? (a > c ? a : c) : (b > c ? b : c);
}

int main(void)
{
	int abc=Max(3, 4, 5);
	cout << abc;

	return 0;
}
