#include <iostream>
using namespace std;
float Zmiana(float x)
{
	return(((x - 32.0) * 5.0) / 9.0);
}
void Wypis(float far)
{
	cout << "W stopniach C to :" << Zmiana(far);
}

int main()
{
	cout << "Podaj tmp fahrenheita:";
	float x;
	cin >> x;
	Wypis(x);

	

	return 0;

}
